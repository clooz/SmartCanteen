# SmartCanteen 待办清单

> 目标：基于当前排查结果，按优先级补齐核心功能、修复风险点、优化体验。

## 分步实施计划（六步走）

按依赖关系拆成 **6 个顺序阶段**；**§9 技术治理** 可与第 1～3 步 **并行** 穿插做，不阻塞功能开发。

| 步骤 | 内容 | 对应待办 | 产出 / 验收要点 |
|------|------|----------|----------------|
| **第 1 步** | 用户端下单闭环 | §1 | 里程碑 A：能选品下单、我的订单可看状态、非订餐时段前端明确禁用 |
| **第 2 步** | 服务端「今日菜单」时区修正 | §2 | 与订单侧业务日一致，跨零点前后不错位 |
| **第 3 步** | 小程序 API 环境化 | §3 | 换机换网只改配置；图片等资源 URL 仍正确 |
| **第 4 步** | 许愿详情与后端状态一致 | §4 | 活动结束时禁用评论/投票等，避免可点但失败 |
| **第 5 步** | 管理端路由懒加载与拆包 | §5 | 主 chunk 下降、首屏与弱网体验提升 |
| **第 6 步** | 体验增强 + 运营可观测 | §6～§8 | 菜单订餐信息条、充值校验、批量操作结果明细等逐项达标 |

**第 1 步（可选）再拆小迭代（便于每日可演示）**

1. `api.js` 增加订单相关方法 + 与后端字段对齐  
2. 下单页（选餐次、选菜、数量、备注、提交）  
3. 「我的订单」列表 + 详情/状态 + `profile` 入口替换「即将开放」  

**并行轨道（建议尽早开始）**

- §9：`.gitignore` 忽略 `admin-web/dist`、避免构建产物进 PR；顺手清理重复路径文件  

**与里程碑对应**

- 完成 **第 1 步** → 里程碑 A  
- 完成 **第 2～3 步** → 里程碑 B  
- 完成 **第 5 步** 并配合 §8 → 里程碑 C 的主要部分；§6～§7、§8 细节属里程碑 C 的持续优化  

---

## P0（必须优先完成）

### 1. 补齐用户端下单闭环
- [x] 新增小程序下单能力（从“今日菜单”发起下单）
- [x] 新增“我的订单”页面（替换“即将开放”占位）
- [x] 对接订单接口（创建订单、查询我的订单、订单状态展示）
- [x] 根据早餐/午餐时段限制下单按钮可用性

**涉及文件（建议）**
- `miniprogram/pages/index/*`
- `miniprogram/pages/profile/profile.wxml`
- `miniprogram/utils/api.js`
- 新增：`miniprogram/pages/order/*`、`miniprogram/pages/my-orders/*`

**验收标准**
- 用户可从菜单页选择菜品并提交订单
- 用户可在“我的订单”查看订单列表与状态（待接单/制作中/可取餐/已完成/已取消）
- 非可订餐时段前端按钮置灰并给出明确提示

---

### 2. 修复服务端“今日菜单”时区问题
- [x] 将 `getTodayMenu` 的日期获取从 UTC 逻辑改为本地日期逻辑
- [x] 与订单侧日期逻辑统一（避免跨天不一致）

**涉及文件**
- `server/src/controllers/menuController.js`
- 参考：`server/src/utils/orderingRules.js`（已有 `localDateStr` 可复用）

**验收标准**
- 本地时区下 00:00 附近不会出现“今天菜单错位”
- 员工端“今日菜单”与服务端实际业务日一致

---

### 3. 小程序 API 地址改为环境化配置
- [x] 移除 `miniprogram/utils/api.js` 中硬编码内网 IP
- [x] 增加 dev / staging / production 预设（`miniprogram/config/env.js` 内改 `CURRENT` 即可切换）
- [x] 支持一键切换并保证图片资源地址同源策略可用（`ASSET_ORIGIN` 随 `API_BASE` 推导）

**涉及文件（建议）**
- `miniprogram/utils/api.js`
- 新增：`miniprogram/config/env.js`（或 `utils/env.js`）

**验收标准**
- 换网络/换机器不需要改业务代码
- 真机调试与测试环境切换成本低

---

## P1（高价值优化）

### 4. 许愿页活动关闭时前端交互一致性修复
- [x] 活动关闭时禁用“评论输入/提交”
- [x] 活动关闭时禁用“投票”按钮（或明确提示不可操作）
- [x] 避免出现“前端可点，后端报错”的体验割裂

**涉及文件**
- `miniprogram/pages/wish-detail/wish-detail.js`
- `miniprogram/pages/wish-detail/wish-detail.wxml`

**验收标准**
- 活动关闭状态下，前端操作状态与后端权限策略一致
- 用户看到的是清晰提示，而不是失败报错

---

### 5. 管理端打包体积优化（路由级拆包）
- [x] 对管理端页面使用 `lazy + Suspense` 按路由拆分
- [x] 将非首屏模块（报表、许愿、充值审核）延迟加载
- [x] 复查打包告警，降低主包体积（主入口 JS 已拆分为多 chunk，仍可按需继续拆 `request`/table 依赖）

**涉及文件**
- `admin-web/src/App.tsx`

**验收标准**
- build 后主 chunk 显著下降
- 登录后首屏加载更快，弱网更稳定

---

## P2（体验增强）

### 6. 用户端菜单页增强“订餐信息可见性”
- [x] 首页展示早餐/午餐当前是否可订、截止时间（有菜单且后端返回 `ordering` 时展示）
- [x] 增加「去下单」主按钮和引导文案（首页有菜单时出现入口条）
- [x] 空状态增加引导动作（如「刷新/去许愿」）

---

### 7. 充值申请提交流程优化
- [ ] 上传前增加图片大小与格式校验
- [ ] 上传过大时给出压缩建议或自动压缩
- [ ] 审核失败时强调原因与修改建议

---

### 8. 管理端批量操作可观测性增强
- [ ] 批量操作后展示成功/失败明细
- [ ] 失败项附带原因（权限、状态冲突、数据不存在等）
- [ ] 支持导出失败列表（可选）

---

## 技术治理待办（建议并行）

### 9. 仓库清理与提交规范
- [x] 将 `admin-web/dist` 等构建产物加入 `.gitignore`（已增加 `admin-web/dist/`；根目录原有 `dist/` 规则保留）
- [ ] 清理重复/无效路径文件（如同名双路径）
- [ ] 统一分支提交流程，避免产物进入业务 PR

---

## 推荐执行顺序（周粒度，与「分步实施计划」对照）

- 第 1 周：**第 1～2 步**（§1 + §2），穿插 §9 中的 `.gitignore`  
- 第 2 周：**第 3～4 步**（§3 + §4）  
- 第 3 周：**第 5 步** + §6（P2-6）  
- 第 4 周：**第 6 步** 剩余项（§7 + §8）+ §9 收尾（清理产物、分支规范）

---

## 里程碑定义

- [ ] 里程碑 A：用户端“可下单 + 可查订单”上线
- [ ] 里程碑 B：时区/环境问题归零，联调稳定
- [ ] 里程碑 C：管理端性能和操作体验可支撑日常运营

