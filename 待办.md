# SmartCanteen 待办清单

> 目标：基于当前排查结果，按优先级补齐核心功能、修复风险点、优化体验。

## P0（必须优先完成）

### 1. 补齐用户端下单闭环
- [ ] 新增小程序下单能力（从“今日菜单”发起下单）
- [ ] 新增“我的订单”页面（替换“即将开放”占位）
- [ ] 对接订单接口（创建订单、查询我的订单、订单状态展示）
- [ ] 根据早餐/午餐时段限制下单按钮可用性

**涉及文件（建议）**
- `miniprogram/pages/index/*`
- `miniprogram/pages/profile/profile.wxml`
- `miniprogram/utils/api.js`
- 新增：`miniprogram/pages/orders/*`（或 `my-orders/*`）

**验收标准**
- 用户可从菜单页选择菜品并提交订单
- 用户可在“我的订单”查看订单列表与状态（待接单/制作中/可取餐/已完成/已取消）
- 非可订餐时段前端按钮置灰并给出明确提示

---

### 2. 修复服务端“今日菜单”时区问题
- [ ] 将 `getTodayMenu` 的日期获取从 UTC 逻辑改为本地日期逻辑
- [ ] 与订单侧日期逻辑统一（避免跨天不一致）

**涉及文件**
- `server/src/controllers/menuController.js`
- 参考：`server/src/utils/orderingRules.js`（已有 `localDateStr` 可复用）

**验收标准**
- 本地时区下 00:00 附近不会出现“今天菜单错位”
- 员工端“今日菜单”与服务端实际业务日一致

---

### 3. 小程序 API 地址改为环境化配置
- [ ] 移除 `miniprogram/utils/api.js` 中硬编码内网 IP
- [ ] 增加 dev/test/prod 环境配置
- [ ] 支持一键切换并保证图片资源地址同源策略可用

**涉及文件（建议）**
- `miniprogram/utils/api.js`
- 新增：`miniprogram/config/env.js`（或 `utils/env.js`）

**验收标准**
- 换网络/换机器不需要改业务代码
- 真机调试与测试环境切换成本低

---

## P1（高价值优化）

### 4. 许愿页活动关闭时前端交互一致性修复
- [ ] 活动关闭时禁用“评论输入/提交”
- [ ] 活动关闭时禁用“投票”按钮（或明确提示不可操作）
- [ ] 避免出现“前端可点，后端报错”的体验割裂

**涉及文件**
- `miniprogram/pages/wish-detail/wish-detail.js`
- `miniprogram/pages/wish-detail/wish-detail.wxml`

**验收标准**
- 活动关闭状态下，前端操作状态与后端权限策略一致
- 用户看到的是清晰提示，而不是失败报错

---

### 5. 管理端打包体积优化（路由级拆包）
- [ ] 对管理端页面使用 `lazy + Suspense` 按路由拆分
- [ ] 将非首屏模块（报表、许愿、充值审核）延迟加载
- [ ] 复查打包告警，降低主包体积

**涉及文件**
- `admin-web/src/App.tsx`

**验收标准**
- build 后主 chunk 显著下降
- 登录后首屏加载更快，弱网更稳定

---

## P2（体验增强）

### 6. 用户端菜单页增强“订餐信息可见性”
- [ ] 首页展示早餐/午餐当前是否可订、截止时间
- [ ] 增加“去下单”主按钮和引导文案
- [ ] 空状态增加引导动作（如“刷新/去许愿”）

---

### 7. 充值申请提交流程优化
- [ ] 上传前增加图片大小与格式校验
- [ ] 上传过大时给出压缩建议或自动压缩
- [ ] 审核失败时强调原因与修改建议

---

### 8. 管理端批量操作可观测性增强
- [ ] 批量操作后展示成功/失败明细
- [ ] 失败项附带原因（权限、状态冲突、数据不存在等）
- [ ] 支持导出失败列表（可选）

---

## 技术治理待办（建议并行）

### 9. 仓库清理与提交规范
- [ ] 将 `admin-web/dist` 等构建产物加入 `.gitignore`
- [ ] 清理重复/无效路径文件（如同名双路径）
- [ ] 统一分支提交流程，避免产物进入业务 PR

---

## 推荐执行顺序

- 第 1 周：P0-1 + P0-2
- 第 2 周：P0-3 + P1-4
- 第 3 周：P1-5 + P2-6
- 第 4 周：P2-7 + P2-8 + 技术治理

---

## 里程碑定义

- [ ] 里程碑 A：用户端“可下单 + 可查订单”上线
- [ ] 里程碑 B：时区/环境问题归零，联调稳定
- [ ] 里程碑 C：管理端性能和操作体验可支撑日常运营

